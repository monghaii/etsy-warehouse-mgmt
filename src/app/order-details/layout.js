export const metadata = {
  title: "Complete your order",
  description: "Enter your order details to customize your product",
};

export default function OrderDetailsLayout({ children }) {
  return children;
}
